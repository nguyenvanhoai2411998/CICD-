name: ECU CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  test:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Install Allure
        run: |
          npm install -g allure-commandline@2.38.0
          allure --version
          allure-combine --help

      - name: Run tests
        run: |
          mkdir -p reports
          python -m pytest \
            --alluredir=allure-results \
            --junitxml=reports/test-results.xml \
            --cov=src \
            --cov-report=term-missing \
            --cov-report=html \
            --cov-report=xml

      - name: Generate Allure report
        id: generate-allure-report
        if: always()
        run: |
          allure generate --clean allure-results -o reports

      - name: Combine Allure report
        if: always()
        run: |
          allure-combine reports
      
      - name: Rename Allure report
        if: always()
        run: |
          mv reports/complete.html reports/reports.html

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            reports/
            htmlcov/
            coverage.xml
          retention-days: 7